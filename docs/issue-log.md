# Super Plumber 统一问题账本（Issue Log）

> **原则**：ADR-0012（过程问题前馈回路，2026-08-30 用户拍板转正）——设计/开发/验证/测试全过程中
> 每一次失败遇到的问题、以及用户提出的修改要求，一律先入本账本（现象/根因/证据/修复方向/紧急度/状态），
> 可行动项转为 `.graph/` 图 **ctx-issue-log 域**的任务节点，按"越紧急越优先"（priority 越小越先）排程，
> 按一般节点方法流转（claim→checkpoint→report→verdict→passed），解决后节点标 passed 并在此销账；
> 不可行动项记录处置理由即可，不建节点。
> **分工**：产品功能缺陷（当前版本代码问题）走 v080 图 L1 修复层 `fix-v080-*`（源账本
> `docs/v0.8.0-issue-log.md`）；过程性问题走本账本 + ctx-issue-log 域。两边 cross-reference，不重复建节点。

| ID | 日期 | 现象 | 紧急度 | 处置 | 状态 |
|----|------|------|--------|------|------|
| IL-001 | 2026-08-30 | **通用性问题（用户 2026-08-30 改题）**：sp-designer 在设计 graph 层数时并"不知道"自己应该置几层——派单与角色提示词都没有置层准则，凭感觉编码。实证：本图扩展时把 level 编码成 1:1 依赖深度产生 33 层（尾部每层 1-3 节点），被用户审出后设计期手工重编为 4 层分期带补救；换一张图还会再犯 | 高（用户改题，流程性准则缺失） | 节点 `il-001-level-encoding`：置层准则成文两处落点——① plumber-design SKILL.md 建图序列节增"置层准则"小节（level=少数有意义的分层带、与分期/领域结构对齐、不用 level 编码依赖深度、层数 ≤5 避免单节点层、出图前先声明置层方案）；② manual §2 design-ops 增明文准则（designer 派单首读必经，等效进系统提示词）；③ doctor level W 项评估。**绑定 g080-design-skill 与 g080-manual-writing 的下游**（SKILL.md 与 manual.md 热点清空后动笔，0.8.0 内消化） | ✅ 已解决（节点 `il-001-level-encoding` 交付，2026-08-30：通用性问题已治理——① SKILL.md 建图序列节（Step 2 派发设计）新增「置层准则」小节，integrations/plugin 副本同步，双副本 diff 仅 3 处既有寻址行；② manual §2.9「置层明文准则」成文（内容与 SKILL 版同源），sync 断言 17 目标全一致；③ doctor W 项评估结论：层数>8 建议增设 W9（事发规模校准）、单节点层判据按宁缺勿滥暂缓（quick 单节点图会必然误报），本节点不实施、留 v0.8.x doctor 批次拍板；ride-along 清账（主控批准）：manual §6.1 补 `approve` 行（27→28）、§6.2 补 `graph_approve` 行（24→25）、§1 计数同步。历史段（33 层事故与手工重编）保留于本行现象列） |
| IL-002 | 2026-08-30 | 批量改标签脚本 glob 未排除 `*.deleted.yaml`，同一 id 被三个来源的 label 反复改写，11 个 g080 节点标签被污染（旧文案+双重前缀），已手工修复 | 高（批量操作前必须守则） | 节点 `il-002-batch-glob-guard`：manual 增批量操作守则（排除 *.deleted* / 先快照 / 批量后 validate+抽查）+ doctor W 项可行性评估；**已绑定 g080-lint-f16 的下游**（等 manual.md 与 sp-check-design.mjs 文件热点清空后动笔，0.8.0 内消化） | ✅ 已解决（节点 `il-002-batch-glob-guard` 交付，2026-08-30：① manual §2.10「批量操作守则（IL-002）」成文——glob 必须排除 *.deleted*（软删历史是审计档案不是数据源）、批量前 `graph snapshot`、批量后 `graph validate` + 抽样 2~3 个被改节点读回字段核对（validate 只保结构不保内容，事故中 validate 全绿污染照样落盘）；sync 断言 17 目标全一致。② doctor W 项评估结论：**不加**（宁缺勿滥）——「活跃节点与 .deleted 同 id 重影」对合法操作必误报：软删后重建同 id 是设计内流程（parser softDelete 二次删除加时间戳后缀即为此备，graph rollback --design-only 软删快照后新增节点同样留下 .deleted 历史），实测本图健康态即有 45 处同 id 重影（11 个 g080 节点 + ~30 条 e080-* 边，default 图 3 处、multitool-refactor 2 处），该 W 项将全量误报；且事故损害是活跃文件内容污染而非文件共存，doctor 经 graph.yaml 引用只读活跃文件、不查 label 内容，加了也拦不住本次事故；引擎侧列表已排除 DELETED_FILE_RE（S3-13），事故在调用侧临时脚本——防线是 §2.10 操作守则而非体检项。附带发现（只读评估产出、本节点不实施）：W5 磁盘扫描硬编码 `.graph/{nodes,edges}` 未走 toGraphDir，多图工作区下静默失效（本次实测 0 条 W5），留 0.8.x doctor 批次处理。③ 事故经验已入工作流记忆（2026-08-30 会话）。历史段保留于本行现象列） |
| IL-003 | 2026-08-30 | `graph_traverse` 默认 max_depth=3、schema 上限 20；深链 33+ 跳，从 fix 层根 max_depth=20 实测只回 49 节点，v093/v094/v100 整段丢失且 `truncated:false` 未如实反映深度截断（用户"五层以后就无法查询了"反馈的实证） | 高（查询正确性） | 节点 `il-003-traverse-depth`：truncated 语义修复（深度截断如实上报）+ 上限上调或 path/find 替代查询（过 DEC-3 判据）+ 深链测试；**已绑定 v081-tooling 的前置**（0.8.1 工具面批次开工前修完） | ✅ 已解决（节点 `il-003-traverse-depth` 交付，2026-08-31：① truncated 语义修复——深度截断与 max_nodes 截断分开记账，响应新增 `truncated_by_depth`/`truncated_by_nodes`，truncated=任一发生，深链截断不再虚报 false；② max_depth schema 上限 20→50（默认 3 不变，改动最小方案过 DEC-3 判据），深链 33+ 跳一次调用可达末端；③ MCP 工具 description 与 manual §6.2 双落点标注新语义与深链使用姿势；CLI 核实无 traverse 子命令故无透传面；tests/mcp/traverse-depth.test.ts 6 用例绿（max_depth=20 复现+50 到末端+max_nodes 截断+schema 边界+upstream+分叉），server.test.ts 回归 9/9 绿，tsc --noEmit 干净） |
| IL-004 | 2026-08-30 | `graph export --mermaid` 输出平铺 graph TD（实测 237 行、101 条 -->，无 subgraph 分组），长图难读，层带语义不呈现 | 中 | 节点 `il-004-mermaid-phase`：按 level 分期带 subgraph 分组 + 大图分段导出选项；**已绑定 v081-tooling 的前置**（与 F15 DECISIONS.md 同属导出模块族，同批次消化） | ✅ 已解决（节点 `il-004-mermaid-phase` passed，2026-08-31：① export --mermaid 默认按 level 分期带 subgraph 分组——知识顶点横切不入带，带升序、带内按 id 排序，输出逐字节稳定（Git diff 噪音可控）；② 分期域名经 `--band-name <level>=<名称>` 显式给，组名显示 L<n> · 域名（图 schema 无 level→域名映射，从数据猜名会误标）；③ 大图分段 `--levels 1,2`（跨带工作流边丢弃、知识顶点仅随保留边带走）；entry/exit 头注释承载原样保留。roadmap-to-1-0-0 实测 4 带 73 工作流节点入带，export-bands.test.ts 22 用例绿 + styling/escaping 回归绿，tsc 干净。遗留记账：DOT 侧 topology.dot 仍平铺（rebuild.ts 越界未动），形状/配色/边样式映射仍与 mermaid 一致） |
| IL-005 | 2026-08-30 | A1 缺陷二次复现：设计定稿保护快照的自动导出再次把 default 图 docs/adr/0001-0003 挤进 `.retired/` 并以 v080 五张覆写同号文件；已按缓解口径 git 复原 | 高 | 已由 L1 修复层 `fix-v080-a1` 覆盖（根治节点），复现取证已补入其 cp1 依据；本条仅记账不建节点 | ➡️ 归 fix-v080-a1 |
| IL-006 | 2026-08-30 | MCP `graph_update_node` 的 plan_description 传参中写 `\n` 会落成字面量反斜杠 n（需真实换行重写） | 低 | 不可行动（调用侧传参纪律，非产品缺陷）：已入工作流记忆；不建节点 | 📝 记录即可 |
| IL-007 | 2026-08-30 | 用户要求"领域图加开发顺序维度"（debug/0.8.x/0.9.x/1.0.0） | 高（用户反馈） | 已实现：4 个分期索引域（ctx-phase-*，boundary 枚举成员）+ relates 顺序链 + 61 个 task 标签加 [版本] 段位前缀 + 真正的分期图例/着色立项为 `v081-webui` cp3；不另建节点 | ✅ 已解决（设计期落地） |
| IL-008 | 2026-08-30 | serve 启动踩坑两次：`dist/cli/index.js` 本身即 graph CLI（无需再带 graph 前缀）；8934 端口被上一会话 serve 常驻占用 | 低 | 不可行动（操作侧知识，serve 已在运行且 watcher 实时推送）：已入工作流记忆；不建节点 | 📝 记录即可 |
| IL-009 | 2026-08-30 | 用户报告 IL001-004 在 webui 点不开详情。根因：`NodeDetail.svelte` 的 linkedAdrs 对"ADR 直挂节点 + ADR 直挂 context 簇传播"双通道命中不去重——il 节点是全图唯一双通道同时命中的节点（adr_0012 既 decides ctx-issue-log 簇又逐个 decides 成员），Svelte each 以 id 为 key 遇重复抛 `each_key_duplicate`，详情面板渲染中断 | 高（用户报告，功能不可用） | 已修复：linkedAdrs 按 id 去重（Set），web-ui 重建，playwright 实测点击 IL-001 详情正常打开（plan/销账/管辖 ADR 全呈现）；图已更名 roadmap-to-1-0-0 后复验通过 | ✅ 已解决（跟进：组件级回归测试缺口随 0.8.x web-ui 工作补） |
| IL-010 | 2026-08-30 | 用户要求图更名为 "roadmap to 1.0.0"。发现 CLI `rename-graph` 早已存在（v0.5.2 起，**刻意仅 CLI 人类通道**，与 delete-graph 同族——工作区级操作不设 MCP）；图名规则 `^[a-z][a-z0-9-]{0,38}$` 不允许点号 | 中 | 已用 CLI 执行更名 v080 → `roadmap-to-1-0-0`（目录+active 随迁+审计事件，serve 先杀后启避免 Windows 目录句柄 EPERM）；节点内导出路径引用同步改 docs/adr/roadmap-to-1-0-0；MCP 通道按既有设计维持缺席——如需 MCP rename 请按 DEC-3/工作区操作边界另行拍板 | ✅ 已解决（MCP 缺席系设计，待拍板项见本行） |
| IL-011 | 2026-08-30 | **通用性问题（用户问"所有边都是 depends_on 是不是问题"，深度研究结论：成立）**：① 选择面虚胖——9 种合法类型中 depends_on 与 validates 机器同构（都进拓扑+门禁），fan_in/fan_out 门禁语义与多条 depends_on 等价（ready 门禁本要求全部门控前驱 passed），fallback/iterates 未实现且 validate 逐条警告，唯一独立特例是 shares_context 与知识边；② 双通道不一致——CLI add-edge 缺省 depends_on 但 MCP graph_add_edge type 必填，sp-designer 走 MCP 建图 → 被迫逐边强判而答案永远相同（本图 106 条工作流边 100% depends_on 实证） | 高（影响所有后续建图） | 节点 `il-011-edge-type-ergonomics`：MCP type 可选化缺省 depends_on（对齐 CLI）+ manual §3 九边速查改写为判据式收敛（默认 depends_on；知识边仅 ADR/领域阶段；fallback/iterates 保留字禁用；validates/fan_* 向后兼容存量、新设计不再用）+ 角色提示词九边选型段收窄。**不选**"不设类型"（知识边/门禁边机器语义真实存在）与"自定义类型"（开放枚举破坏 schema 确定性）。**已绑定 v081-tooling 的前置** | ✅ 已解决（节点 `il-011-edge-type-ergonomics` 交付：MCP `graph_add_edge`/`graph_batch_create` edge type 可省略缺省 depends_on（zod default，对齐 CLI 既有默认，双通道一致化），tests/mcp 新增用例覆盖缺省落盘与显式类型不受影响；manual §3 改写为「边类型判据式速查」——默认 depends_on + **方向约定成文**（source 是被依赖的前置、target 是等待方，写反=门禁倒挂）+ 知识边仅 ADR/领域建模阶段 + shares_context 非门禁标注 + validates/fan_* 向后兼容存量新设计不再用 + fallback/iterates 保留字禁用，sync 断言 17 目标全一致；sp-designer 双副本（§1.3 + 附录选型段）同步收窄，diff 仅寻址行差异；全量 560 测试绿。IL-013 方向约定已随 §3 落地（SKILL 建图序列节按文件边界留待 IL-001 时点）；execution_report 已交、待主控 verdict） |
| IL-012 | 2026-08-30 | **通用性问题（IL-011 研究结论③单独立项）**：跨 context 契约逐边手写——本图 74 条 contract 高度公式化，设计者每条边写一段 JSON；契约本质是"两个 bounded context 之间的关系"属性，逐边声明是错误粒度 | 中 | 节点 `il-012-contract-by-context-pair`：context 顶点（或图级）声明 context 对默认契约，跨 context 边自动继承、单边可覆写；validate 改为"context 对无声明且边无 contract 才警告"；双通道；存量图零迁移。预期 74 条逐边契约 → 约 12 条声明。**已绑定 v082-tooling 的前置**（与 class 字段同属 schema 收口批次） | ✅ 已解决（节点 `il-012-contract-by-context-pair` 交付：context 顶点 `contracts:[{to,contract}]` 声明（挂 source 侧、to 指向消费侧），validate 逻辑继承 + 单边覆写优先 + 声明侧悬空 error/重复 warning；CLI `--contract-add` 与 MCP `contract_add` 双通道；本图 87 条逐边契约零迁移原样通过、全量 559 测试绿；execution_report 已交、待主控 verdict） |
| IL-013 | 2026-08-30 | **depends_on 方向约定反直觉**：source 是被依赖的前置、target 是等待方（箭头从前置指向依赖者）——主控绑定 IL 时写反三条边（il-001/il-002 侧），若非复查会静默卡死 0.8.0 的 design-skill/manual-writing/lint-f16，已当场换向修复 | 高（方向错=门禁倒挂） | **已由 IL-011 覆盖**：其 manual §3 判据式改写与 SKILL 建图序列节新增"方向约定"条款（plan 板块④，事故实证已写入）；本条记录即可，不另建节点 | ✅ 记录即可（归 IL-011 消化） |
| IL-014 | 2026-08-31 | F16 c 类词表漏报：verify-lint 验收用计划自带种子文案「确保合理优化整体质量」三通道一致不命中——词表只收带「地」形态与整词「确保质量/保证质量」，裸词干「合理」逃逸；这类主观承诺恰是 c 类规则的目标猎物（主控裁决：真召回洞，走第二循环修复而非改种子回避） | 高（验收节点当场拦截） | 第二循环修复（verify-lint 节点内闭环，g080-lint-f16 已 passed 故不重开节点）：style-lint.ts 新增 `UNVERIFIABLE_STEMS_ZH` 裸词干表（合理/正确/适当/妥善/恰当/充分）+ 守卫正则 `(?<![不非])词干(?![性化界地])`（前邻不/非、后邻性/化/界/地 均放行，防「不合理/合理性/合理化」误报与带地双报）；tests/core/style-lint.test.ts 补种子回归+守卫三连反例；三通道（CLI/MCP/W8）同源受益零脚本改动；全套 605 用例绿，交叉复核独立重验通过 | ✅ 已解决（2026-08-31 第二循环交付并经 cross_review 签署；归档于 g080-verify-lint 执行报告） |
| IL-015 | 2026-08-31 | 交叉复核时外部 MCP 驱动脚本路径参数因 shell 转义展开为空，server 按 roots 回退落到 cwd（仓库根），对真图 roadmap-to-1-0-0 误写 `review{self, quick-op}` + 1 条 design_approved 事件；~2 分钟内发现并外科式还原（writeGraphCore 确定性全量序列化，还原后字节等价；validate ok、events/design_approved 计数 0——主控已独立复核真图零污染） | 中（工具行为是设计，事故在调用侧） | 不可行动（产品侧不改）：roots 回退是 MCP 设计行为；教训=外部一次性驱动必须显式传 `--root <目录>` 并应答 roots/list，已入工作流记忆；不建节点 | 📝 记录即可（事故现场备份 /tmp/graph.yaml.accidental-approve.bak 等，主控复核通过） |
| IL-016 | 2026-08-31 | **发版版本面只改 package.json 一处**：0.8.0 发版（aac3130）对齐了 integrations/plugin/.claude-plugin/plugin.json 但漏掉仓库根 .claude-plugin/marketplace.json（停在 0.6.3，用户审出）；manual.md 版本锚点同批发现停在 0.6.0。根因=发版清单没有「版本面同步」步骤、发布门禁也不查 manifest 版本一致性 | 中（用户可见的元数据失真，不影响运行） | 当场改齐（2026-08-31）：marketplace.json → 0.8.0；manual 锚点 → 0.8.0（shared 正本 + sync 传导 17 目标一致）。防复发立节点 `il-016-manifest-version-gate`：版本面一致性断言进门禁（package.json 与 .claude-plugin/marketplace.json、integrations/plugin/.claude-plugin/plugin.json，0.9.5 起 + .codex-plugin/plugin.json 与 .agents/plugins/marketplace.json 共五方）+ 发版清单补「版本面同步」步骤行；**已绑定 v081-tooling 的前置**（与 il-003/004 同批，0.8.1 开工前清偿） | ✅ 已解决（2026-08-31 il-016-manifest-version-gate passed：版本面一致性断言进门禁 sync-integrations.mjs --check（manifest 存在才校验、0.9.5 起五方、不一致 exit(1) 列差异），为 prepublishOnly 第一段自动生效；README 新增「发版清单」小节含版本面同步步骤行；反例试验 0.6.3 错配 exit(1)、还原后正向 exit(0) 实证） |
| IL-017 | 2026-08-31 | **sp-traverse.mjs 脚本层语义漂移**：`integrations/shared/sp-scripts/sp-traverse.mjs` 内嵌自研 DFS 副本，无 truncated 输出、无 max_depth 上限语义——il-003 修复 MCP traverse（truncated_by_depth/nodes 如实上报 + 上限 20→50）后，脚本通道与 MCP 通道对同一查询给出不同语义（三访问层一致性破口；il-003 执行者按 adr_0012 前馈回路上报） | 中（脚本通道静默落后于 MCP 语义，不影响当前主链路） | 建节点 `il-017-sp-traverse-drift`，**绑定 v094-scripts 的前置**（S03 七脚本收敛 sp.mjs 薄封装 ≡ CLI 语义面时一并收编，不提前单修避免双份返工） | 🔧 节点 pending（0.9.4 清偿） |
| IL-018 | 2026-08-31 | **DOT 导出与 mermaid 分带不对称**：il-004 为 mermaid 加 level 分期带 subgraph 分组与 --levels 裁剪后，DOT 侧 topology.dot（rebuild.ts）仍平铺——形状/配色/边样式映射仍一致，分组结构不一致，手册 §2.5「同一映射」按结构解读有措辞漂移（il-004 执行者按 adr_0012 前馈回路上报） | 低（呈现层差异，无语义影响） | 记录即可，不建节点：DOT 分组待真实需求再立项；近期若动手册，可把 §2.5 措辞改为「映射一致、分组仅 mermaid」 | 📝 记录即可（不建节点） |
| IL-019 | 2026-08-31 | **web-ui 两处边角（v081-verify 交叉验证观察项，super-mario 裁记录即可）**：① 纯知识顶点图（0 工作流节点）勾选 map 透镜仍提示「所有 map 透镜已关闭」且画布不渲染，加入首个工作流节点后恢复；② 无成员 context 不渲染星云但图例仍列出（簇壳渲染需 ≥1 成员，疑似设计内行为） | 低（真实图不存在 0 工作流节点场景；②疑似设计内） | 记录即可，不建节点：两处同为 backlog 低优先级候选，若后续做（透镜空态文案修正 + 空 context 占位壳）同批进 0.8.x web-ui 批次；附带数据清洗——ctx-webui「Avoid 呈现」定义正文中置 `Avoid:` 字样已改写（快照 52s943 保底，rebuild+validate 过） | 📝 记录即可（backlog 候选） |

**销账纪律**：节点 passed 后把对应行状态改为 ✅ 并注明节点 id；新增问题从下一行追加，ID 递增（IL-009…）。
