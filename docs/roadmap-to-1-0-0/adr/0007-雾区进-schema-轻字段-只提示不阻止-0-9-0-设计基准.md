# 0007 — 雾区进 schema：轻字段 + 只提示不阻止（0.9.0 设计基准）

graph 级可选 fog 字段（id/描述/毕业条件）+ fog_graduated 审计事件 + validate 雾区提示（只提示不阻止）；program 档 chart the graph / work the graph 两模式附着其上；毕业动作复用 DEC-7 amend 影响评估。

**Status：** accepted

**Context：** 「图一次画完」迫使认知未到处假装精确；0.8.2 WF08 雾区约定版试跑（v082-fog-recon）提供手感输入，schema 版以试跑结论为准。

**Considered Options：** 永远只做约定版（雾区毕业率不可观测，否）；重量级研究工作流引擎（违反轮子边界，否）；本方案（采纳）。

**Why：** schema 字段让雾区可观测可毕业（Q3 指标），validate 只提示守住 DEC-3「不阻止」红线。

**Consequences：** 读接口返回雾区概要；星空虚线云团呈现；破坏「轻量」心智的风险以约定版先行缓解。

> 本文由 `graph export` 从图顶点 adr_0007 生成；改图不改文，重新导出即覆盖。
