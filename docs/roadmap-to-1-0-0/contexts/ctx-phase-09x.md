# 开发顺序③：0.9.x 结构升级期（ctx-phase-09x）

索引型上下文（分期域，不承载节点归属）：盘点 0.9.x 五个版本的全部工作——0.9.0「雾中绘图」（雾区 schema + chart/work 两模式）+ 0.9.1「人机分工进调度」+ 0.9.2「渐进审批」+ 0.9.3「纸面边清偿」+ 0.9.4「单真相源重组」（主线 B 主体）。成员（25）：0.9.0 = v090-fog-schema、v090-chartwork、v090-fogui、v090-verify、v090-release；0.9.1 = v091-tooling、v091-skills、v091-verify、v091-release；0.9.2 = v092-progressive、v092-verify、v092-release；0.9.3 = v093-fallback、v093-verify、v093-release；0.9.4 = v094-restructure、v094-oneline、v094-attachments、v094-scripts、v094-hooks、v094-agents、v094-disciplines、v094-gov、v094-verify、v094-release。前置：ctx-phase-08x。触达技术域：四域全部。跨版本契约输入：v082-fog-recon 试跑结论→v090-fog-schema；v082-amend-skill 协议→v090-chartwork 毕业复用；v082-join→v094-agents（sp-executor 引用）。后续阶段：ctx-phase-100。

## 术语表

- **结构升级期（0.9.x）**: 结构升级期是 0.9.x 的阶段定位——MCP/CLI 能力先行、同版本内工作流话术立即跟上（雾区/等真人/渐进审批的话术没有字段就无处附着），承载主线 A 主体（雾区）与主线 B 主体（单真相源重组）。

> 本文由 `graph export` 从 context 顶点 ctx-phase-09x 生成（节点即文档，图是真相源）。
