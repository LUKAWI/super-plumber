# pi 集成上下文（ctx_pi）

pi 工具的原生集成文本资产：.pi/agents 两份角色提示词与 .pi/skills 双 skill 的编排化重写。不含 claude/zcode 的适配格式（归各自上下文），不含命令语法正本（归 shared）。

## 术语表

- **提示词瘦身**: 迁出 CLI/MCP 语法对照表、但完整保留身份+流程+铁律的角色载体改写方式
- **编排剧本**: 只承载阶段推进硬 gate、派单模板（含文件边界）、solo 分支三种编排语义的 skill 形态

> 本文由 `graph export` 从 context 顶点 ctx_pi 生成（节点即文档，图是真相源）。
