# Web 可视化（ctx_viz）

多图并行渲染：递归监听全部图目录、ws 消息按图路由、折叠任务栏选图器；纯只读观察窗，切图不影响 CLI/MCP 状态

## 术语表

- **分桶 store**: 前端按图名分桶持有状态，当前图渲染、后台图静默热更新（切过去即时新鲜）
- **图选择器**: 折叠任务栏/开关式选图：有几张渲染几张，初始选中=工作区 active

> 本文由 `graph export` 从 context 顶点 ctx_viz 生成（节点即文档，图是真相源）。
