# claude-code 集成上下文（ctx_claude）

面向 Claude Code 的 .claude-plugin 插件包（plugin.json + agents/skills/commands/.mcp.json）与仓库根 marketplace.json 的组装与清单管理。内容基线来自 pi 终稿，自身不发明流程语义。

## 术语表

- **格式适配**: 仅限 frontmatter 字段映射、路径指向与双语措辞微调的内容搬运，禁止语义改动
- **四项验收**: 装载验收四件套：skill 可触发、command 注册、agent 出现、MCP 自动连接

> 本文由 `graph export` 从 context 顶点 ctx_claude 生成（节点即文档，图是真相源）。
