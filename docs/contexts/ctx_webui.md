# Web 可视化（ctx_webui）

纯只读可视化：map 过滤与 D3 渲染；零写路径（裁决控制台是未来可选演进）

## 术语表

- **透镜（map lens）**: 按顶点类型派生的视图过滤：工作流图/领域图任意子集勾选叠加；单独看是一等能力
- **簇壳（cluster hull）**: 叠加视图中 context 的空间包裹呈现（D3 polygonHull+着色）；表达归属不画归属连线

> 本文由 `graph export` 从 context 顶点 ctx_webui 生成（节点即文档，图是真相源）。
