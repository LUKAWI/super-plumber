# 命令行通道（ctx_cli）

人类运维通道：命令解析与输出格式化；无业务规则（全部下沉 core）；force 等越权操作仅此通道可用

## 术语表

- **命令组**: graph adr 这类带子命令的命令；区别于单动作命令（如 update-status）
- **退出码**: 错误路径必须非 0——供脚本/CI 判断；警告不改变退出码

> 本文由 `graph export` 从 context 顶点 ctx_cli 生成（节点即文档，图是真相源）。
