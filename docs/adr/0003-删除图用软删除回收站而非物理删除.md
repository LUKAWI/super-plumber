# 0003 — 删除图用软删除回收站而非物理删除

graph delete-graph --confirm 把图目录移入 .graph/.trash/<名>-<时间戳>/（可手工救回），不物理 rm；拒删最后一张图、拒删 active 指向的默认图。

**Status：** accepted

**Context：** 图删除是不可逆操作。备选：直接 rm -rf；先强制快照再删；回收站。

**Considered Options：** rm -rf→不可逆且无审计; 快照后删→快照救援路径复杂（整图恢复非原语）; .trash 软删除←选中（rename 原子、可手工救回、workspace-events 留痕）

**Why：** 与产品既有软删除传统（nodes 的 .deleted.yaml）同构：删除可逆、可追溯；rename 同卷原子，无需复制大目录。

**Consequences：** .trash 需定期人工清理（暂无 GC）；watcher 必须忽略 .trash（否则删除事件触发全量推送）；救回靠手工 mv（指引写入命令输出）

> 本文由 `graph export` 从图顶点 adr_0003 生成；改图不改文，重新导出即覆盖。
